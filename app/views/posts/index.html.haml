- if user_signed_in?
  %p#intro
    Hello
    = current_user.name
    %br/
    %span
      Share your inspiration and see what inspiring others.
- else
  %p#intro
    What's your Muse?
    %br/
    %span
      Share your inspiration and see what inspiring others.

- @posts.each do |post|
  = link_to (image_tag post.image.url(:small)), post
  %h3= link_to post.title, post
  %p
    = post.comments.count
    Comments
  %p
    = post.get_likes.size
    Likes
  %p
    = post.get_dislikes.size
    Dislikes

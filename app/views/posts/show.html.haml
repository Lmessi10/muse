= image_tag @post.image.url(:medium)
%h1= @post.title
%p= @post.link
%p= @post.description
By
%p= @post.user.name

= link_to "Edit", edit_post_path
= link_to "Delete", post_path, method: :delete, data: { confirm: "Are you scure?"}
= link_to "Home", root_path